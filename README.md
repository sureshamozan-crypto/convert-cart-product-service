@"
# 🛍️ Product Service (Node.js + Express)

A RESTful Product Service built using Node.js and Express.js.

## 🚀 Setup

1. Clone repo
2. Run `npm install`
3. Create `.env` file
4. Run `npm start`
"@ | Out-File -Encoding utf8 README.md
